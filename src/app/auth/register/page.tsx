import { RegisterForm } from '@/features';

export default function RegisterPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-bg dark:bg-black">
      <RegisterForm />
    </main>
  );
}
